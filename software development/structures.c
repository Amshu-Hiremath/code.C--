#include<stdio.h>
void main()
{
    struct book
    {
        char name [20];
        float price;
        int pages;
    };
    struct book b1=("Basic Electronics",450.00,560);
    struct book b2=("Physics", 350.00 300);
}